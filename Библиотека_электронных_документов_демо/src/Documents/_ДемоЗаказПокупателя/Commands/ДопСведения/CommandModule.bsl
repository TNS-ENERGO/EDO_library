
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ИмяФормы = "РегистрСведений._ДемоДополнительныеСведения.ФормаСписка";
	СтруктураОбъекта = Новый Структура("Объект", ПараметрКоманды);
	
	ОткрытьФорму(
		ИмяФормы,
		Новый Структура("Отбор", СтруктураОбъекта),
		ПараметрыВыполненияКоманды.Источник,
		ПараметрыВыполненияКоманды.Уникальность,
		ПараметрыВыполненияКоманды.Окно);
	
КонецПроцедуры

#КонецОбласти
